package orwashere.springboot;

import org.springframework.social.twitter.api.Twitter;

/*import javax.persistence.Entity;
import javax.persistence.Id;
import javax.persistence.JoinColumn;
import javax.persistence.ManyToOne;*/

/**
 * Created by fdirlikl on 1/21/2016.
 */
//@Entity
public class Followers {

   /* @Id
    Long twitterId;

    @ManyToOne(targetEntity = TwitterProfile.class)
    @JoinColumn(name = "parentProfile.id")
    TwitterProfile parentProfile;

    public Followers(Long twitterId)
    {
        this.twitterId = twitterId;
    }

    public Followers(Long twitterId, TwitterProfile parentProfile)
    {
        this.twitterId = twitterId;
        this.parentProfile = parentProfile;
    }

    public Long getTwitterId() {
        return twitterId;
    }

    public void setTwitterId(Long twitterId) {
        this.twitterId = twitterId;
    }

    public TwitterProfile getParentProfile() {
        return parentProfile;
    }

    public void setParentProfile(TwitterProfile parentProfile) {
        this.parentProfile = parentProfile;
    }*/
}
